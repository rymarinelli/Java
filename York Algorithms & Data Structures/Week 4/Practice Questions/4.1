COUNTING-3 (n)
1 return n × n

Item	                                         Pseudocode convention	          Java syntax
Algorithm declaration		                      CAPITALS                     
Block structure		                            white space                             {}
Loop & loop condition		                      while/for condition              for conditon{}
comment		                                     //                               //
Multiple assignment		                        ==                           
Variable scope		                                                         
Array access		                             array[i]                      
Object, attribute and reference		            x.y                          
Parameter passing by value or reference		                                 
Return statement		                          return                          return
Short circuiting		                          X is evaluated first            X is evaluated first
Comparison operators		                      and      
Error message		                              error                           exception

LINEAR SEARCH(A,n,x)
index = 1
while index ≤ n and A[index]  ≠  x
  index = index + 1
if index > n
    index = 0
    return index

Question 3:
Write an algorithm which takes an array of n integers as input and output the maximum element of the list.
Analyse its time complexity and express it using asymptotic notations.

SEARCH SORT(A,n,x)
index = 1 
while index  ≤ n and A[index]  ≠  x
  if A[index] < A[index -1 ]
     A[index -1] = A[index]
return A[index.length]     

Big-Oh O(n)
Ω(1)
Time Complexity N + a

  
